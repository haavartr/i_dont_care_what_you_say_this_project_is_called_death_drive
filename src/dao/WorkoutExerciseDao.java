package dao;

import entities.WorkoutExercise;

import java.util.List;

public interface WorkoutExerciseDao {  // Selecting, deleting and updating is done through the children
    void createWorkoutExerciseTable();
}
